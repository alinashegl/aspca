/**
 * Created by barne on 6/1/2021.
 */

({
    doInit : function(cmp,event, helper ) {
        helper.processingProcess(cmp, 'init');
    } ,
    handleSelect : function(cmp, event, helper) {
        helper.processingProcess(cmp, 'handleSelect');
    },
    handleSelect2 : function(cmp, event, helper) {
        helper.processingProcess(cmp, 'handleSelect2');
    },
    handleSelect3 : function(cmp, event, helper) {
        helper.processingProcess(cmp, 'handleSelect3');
    },
    handleSelect4 : function(cmp, event, helper) {
        helper.processingProcess(cmp, 'handleSelect4');
    },
});